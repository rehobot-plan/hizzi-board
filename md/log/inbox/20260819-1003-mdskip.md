# 20260819-1003-mdskip — MD 만 바뀐 커밋은 빌드를 안 세운다 (히찌보드)

레인 `lane/mdskip` · 워크트리 `/Users/chillkim/hizzi-board-wt/mdskip` · 기준 커밋 `362bfd4` · 커밋 `08002b9`

---

**1. 탐색·검색**

Vercel 원문 셋(2026-08-19 · `WebFetch`)에서 잰 것 — 종료값 0 = 빌드 스킵 · 1 이상 = 빌드 진행(오류로 죽으면 자동으로 빌드 쪽이다) · 부르는 자리는 Root Directory · 클론은 `git clone --depth=10` 이라 이력이 열 개뿐 · `VERCEL_GIT_PREVIOUS_SHA` 는 그 가지의 **마지막 성공 배포** 커밋이고 Ignored Build Step 이 있을 때만 노출된다.

저장소 내부 — **이 저장소에는 `vercel.json` 이 없었다**(이력 0건 · 실측). 빌드가 `md/` 를 안 읽는 것도 확인했다(`src`·`next.config.js`·`scripts` 어디에도 `md/` 를 읽는 자리가 없다).

**2. 구현**

`vercel.json` 을 새로 세웠다. 판정은 르호봇과 같은 글자다 — 이전 배포 커밋과 HEAD 사이 바뀐 경로가 **전부** 루트 `*.md` 이거나 `md/` 아래 `.md` 일 때만 스킵하고, 모르는 자리는 전부 빌드로 떨어진다(fail-open).

**두 저장소가 같은 글자를 갖는다** — 층에 한 벌 두는 방식을 못 쓴다. Vercel 은 그 저장소만 클론하므로 빌드 컨테이너에 층이 아예 없다. 앱 무관 도구를 층에 두는 규약(`harness.md` 3-4-(2))이 이 자리에는 원리상 안 선다.

**3. 빌드 검증** — PASS. `npm run lint` PASS(경고 7 · 전부 이 회차 밖 기존 파일) · `npm run build` PASS(19 화면) · `npm run test:unit` PASS(6파일 48검사).

되살림 — **이 저장소 `vercel.json` 의 실제 문자열을 읽어 국소에서 실행했다**(옮겨 적지 않았다 · R-MEASURE).

| 심은 것 | 기대 | 결과 | 판정 |
|---|---|---|---|
| MD 만 바뀐 커밋 | 스킵 | 스킵(0) | OK |
| 코드가 바뀐 커밋 | 빌드 | 빌드(1) | OK |
| MD 와 코드가 함께 | 빌드 | 빌드(1) | OK |
| 레인 가지 + 코드 | 빌드 | 빌드(1) | OK |
| 레인 가지 + MD 만 | 스킵 | 스킵(0) | OK |
| 루트 CLAUDE.md 만 | 스킵 | 스킵(0) | OK |
| MD 삭제만 | 스킵 | 스킵(0) | OK |
| md/ 밖의 .md | 빌드 | 빌드(1) | OK |
| 이름에 빈칸 든 파일 | 빌드 | 빌드(1) | OK |
| md/ 아래 .md 아닌 파일 | 빌드 | 빌드(1) | OK |
| 기준 커밋 없음(빈 값) | 빌드 | 빌드(1) | OK |
| 기준 커밋이 이력 밖 | 빌드 | 빌드(1) | OK |

표본 12 · 어긋남 0. 틀린 줄 둘을 일부러 먹여 계측이 우는 것도 확인했다(가지 기반 옛 줄 4건 붉음 · fail-closed 로 뒤집은 줄 3건 붉음).

**4. Codex 리뷰** — PASS(`exit=0 reason=done` · 로그 `.harness/mdskip-hizzi.log`). 차단 결함 0. 원문 — "The Vercel ignore command correctly skips deployments only when all changed files are Markdown files in the root or under md/, while conservatively proceeding with deployment when comparison data is unavailable or any other file changed."

**리뷰 시야 밖** — 이 변경이 겨누는 것은 Vercel 빌드 컨테이너의 행동이라 리뷰 대상 밖이다. 그 몫은 되살림으로 따로 쟀고 라이브 판정은 안 쟀다.

**5. E2E** — 비해당. 변경 파일이 `vercel.json` 하나이고 브라우저 동작에 안 닿는다. 이 앱은 1-5 도구가 아직 없다("없음" — 오너 육안 · `harness.md` 3-7 표).

**6. 배포** — 레인 미리보기까지. 가지 push 완료(`lane/mdskip` → origin · 새 가지). 고객 배포 안 밟았다.

**미리보기 주소를 못 냈다** — 이 저장소는 **미리보기 빌드가 실패 상태로 서 있다**(직전 회차 `buildfail` 이 "Preview 환경변수 0" 으로 원인을 적어 두었다 · 이 회차 밖이다). 안 잰 것을 통과로 적지 않는다.

**7. 닫는 todo 항목** — 해당 없음.

**8. 미러 동기화** — 비해당 — 이 앱 문서를 미러가 안 담는다(`harness.md` 3-7 표). 작업 레인이기도 하다.

**9. 교훈 반영** — **앱 문서에 못 세웠다(막힘).** 지시서 ⑸ 가 요구하는 자리가 이 저장소에서는 셋인데 셋 다 막힌다: `md/core/*.md` 는 `lane/hizzifix` 점유(R-LANE.2) · 새 파일을 그 폴더에 세우는 것도 그 무늬 안 · 진입점 `CLAUDE.md` 는 담는 것이 넷뿐이라 규약 밖(`rules.md` 6-1-(1)). **무엇이 있으면 잴 수 있나** — hizzifix 가 그 무늬를 놓거나, 이 앱에 `md/ops/` 갈래가 서면 그 자리에 르호봇 `md/ops/deploy-skip.md` 와 같은 한 장을 세울 수 있다.

**10. 기계 쪽을 늘렸으면 그것이 적힌 자리를 함께 본다** — 늘린 것은 `ignoreCommand` 판정 하나다. 이 저장소에 그 이름을 지목하는 자리는 없었다(신설이라 선례 0). 층 쪽 한 문장은 점유 밖이라 못 따라갔다.

**11. 우편함 걷기** — 비해당 — 작업 레인.

---

## 통합 레인이 받을 것

1. **앱 문서 반영이 이 레인에서 원리상 못 선다**(위 9). 지시서의 "hizzifix 파일은 건드리지 마라"와 "앱 문서에 적어라"가 같은 파일을 가리킨다. 회차를 다시 잡거나 hizzifix 통합 뒤로 미루는 판단이 필요하다.
2. **원장 점유 범위는 맞다** — 이 레인이 만진 것은 `vercel.json` 하나다(르호봇 쪽은 둘 늘었다).
3. **이 앱 미리보기는 이 회차 전부터 실패 상태다** — 이 변경과 무관하고, 그래서 라이브로 판정을 못 쟀다.
