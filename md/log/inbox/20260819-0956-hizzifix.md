# hizzifix — 죽은 지목 걷기와 배포 절 실재 맞춤 (2026-08-19)

레인 `lane/hizzifix` · 기준 `362bfd4` · 커밋 `c52f4cc` · 작업지시서 전언(오너 승인).

**done 줄** (최신이 맨 위로 붙인다)

- [2026-08-19] 죽은 지목을 걷고 배포 절을 실재에 맞췄다 (hizzifix · `c52f4cc` · 오너 승인) — 영향 파일 5개, 층 장 재배치 잔존 넷을 고치고 `master-debt` 둘은 다른 문서를 가리켜 안 고친 채 머리에 가르는 줄을 세웠으며, 실패한 Preview 가 200 을 낸다는 실측을 문서가 지게 했다.

---

## 완료 보고 11항목

### 1. 탐색·검색

**저장소 내부 — 전수 조사를 축 넷으로 갈라 돌렸다.** 무늬 하나로는 못 서는 것이 이 회차의 전제였다(지시서가 그 사각 둘을 미리 들었다).

| 축 | 무늬 | 무엇을 잡나 | 잡힌 줄 |
|---|---|---|---|
| 1 · 낱말 | `harness\|하네스` | `harness.md 3장` 꼴 **1단 지목** — 좌표 무늬로는 안 잡힌다 | 전체 78줄(archive 포함) |
| 2 · 좌표 | `거버넌스 [0-9]` · `rules\.md [0-9]` · `CLAUDE\.md \[` 외 8 | **문서 이름 없이 숫자만** 지목하는 줄 | 13줄 |
| 3 · 파일 | `git grep` 추적 전수 + `status --porcelain -uall` | **확장자로 좁히면 생기는 사각** — 안 보는 파일 | 미추적 0건 · `.harness/` 추적 2건 확인 |
| 4 · 순수 좌표 | `(^\|[^0-9A-Za-z])[1-6]-[0-9]([^0-9A-Za-z]\|$)` | 낱말 없이 **좌표만** 있는 줄 | 47줄(거짓 적중 포함) |

**범위** — 워크트리 전체(추적 파일 전수), 판정 대상에서 `md/archive/` 와 `md/log/done.md` 제외. **둘을 뺀 사유는 이력이라 그때 서술을 안 고치는 것**이고, **세기는 세어 값으로 안다**(축 1이 archive 30여 줄을 실제로 잡았다).

**거짓 적중을 걷은 값** — 축 4의 47줄 중 **실제 어긋남은 넷**이다. 걷어 낸 부류: 자기 문서 내부 절 번호(`master-operator.md` 12줄 · `master.md` 6줄) · 지금도 맞는 층 좌표(`1-1 탐색`·`1-6 배포`·`1-4 Codex` 등 14줄) · 대괄호 부류(별건 · 아래 10) · `master-debt` 부류(별건 · 아래 3).

**외부 조사 — 안 했다. 사유는 '공장의 판단'이다.** 코드 파일 무변경 MD 전용 회차라 R-EXT 가 요구하는 자리가 아니고, 훅도 코드 파일 편집에만 걸린다. '도구 부재'도 '못 함'도 아니다.

### 2. 구현

**변경 파일 5개 · 78 insertions · 13 deletions**(`git log -1 --stat` 실측).

**(1) 층 장 재배치 잔존 넷** — 지시서가 다섯으로 줬고 **넷이 맞다**(아래 3이 그 갈래).

| 파일 | 옛 좌표 | 새 좌표 | 층을 가리키는 근거 |
|---|---|---|---|
| `md/core/coding-rules.md` | 하네스 **3-1** 탐색 | 1-1 | 문장에 "거버넌스" |
| `md/core/session.md` | (도구는 **1-7** 표) | (공정은 1장 · 앱별 도구는 3-7 표) | 표 머리가 "부모 폴더 `dev/` 의 거버넌스 저장소" |
| `md/core/session.md` | 하네스 **3-6** | 1-6 | 같은 문서 · 완료 보고 자리 |
| `md/log/todo.md` | harness.md **3-6** | 1-6 | 항목이 "(거버넌스 잔여)" |

**(2) `md/core/master-debt.md` — 좌표는 안 고치고 머리에 가르는 줄을 세웠다.** 아래 3 참조.

**(3) `md/core/master.md` 10장 — 절 셋을 고치고 둘을 세웠다.**
- CLI 블록: 배포 줄과 클린 빌드 줄을 걷고 게이트 셋을 남겼다. **걷은 사유를 함께 적었다** — 낡아서가 아니라 공장이 원리상 못 치는 줄이어서다.
- 미리보기 절: **"열리지 않는다" → 실측(200)** 으로 고치고, 상태 코드로 못 가른다는 것과 **본문이 판번호인지 먼저 묻기**를 세웠다.
- 미리보기 절 머리: "세울 것은 없다 — Preview 가 스스로 선다"가 같은 절 아래 문단("원리상 안 선다")과 정면으로 부딪히던 것을 갈랐다(**이어 읽기가 잡았다** — 아래 3).
- **고객 배포 절 신설**: `git push origin master` · CLI 는 비상 통로 · MD 전용 회차는 이 절 비해당.
- **1-5 E2E 절 신설**: 자산과 없는 것을 값으로 갈라 적었다. **부르는 줄은 안 지어 냈다.**

**(4) `md/log/todo.md`** — 대괄호 등재를 실재에 맞추고, `playwright-login` 항목의 죽은 지목을 걷고 사실만 남겼다.

### 3. 빌드 검증 — PASS

**셋을 하나씩 단독으로 불렀다**(파이프 없음). 자리는 `/Users/chillkim/hizzi-board-wt/hizzifix`.

| 게이트 | 값 | 부류·단위 |
|---|---|---|
| `npm run lint` | 경고 7 · 오류 0 | eslint 진단 건수 |
| `npm run test:unit` | 6 파일 48/48 통과 · 0.97초 | vitest 검사 건수 |
| `npm run build` | 19 라우트 프리렌더 성공 · 종료값 0 | next build 라우트 수 |

**두 번 돌렸다** — 편집 뒤 한 번, **마지막 두 편집(이어 읽기가 잡은 자리) 뒤 커밋 직전에 다시 한 번**. 층 1-3-6 이 "게이트는 작업 트리를 재고 커밋을 안 잰다"고 적어, 마지막 편집이 게이트 뒤에 있으면 잰 값이 커밋될 것과 다르다. 두 회차 값이 같았다.

**이 셋은 MD 를 안 잰다.** 대상이 `src/`·`tests/` 라 이번 변경분은 **원리상 이 게이트 시야 밖**이다 — 초록은 "MD 가 성하다"가 아니라 "MD 를 고쳐도 코드가 성하다"이다. **이 저장소에 MD 를 재는 게이트는 없다**(층 R-LANG 정적 게이트는 르호봇 것이고 층 `md/` 를 겨눈다).

**자리 실측 하나** — 워크트리는 층 밖이라 `cd` 가 매 호출 뒤 리셋됐고 **하네스가 그때마다 알렸다**(`Shell cwd was reset to /Users/chillkim/dev`). 층 `harness.md` 3-5-(8)이 적는 두 갈래 중 **알리는 쪽**이다. 그래서 게이트마다 `cd {절대경로} &&` 를 다시 붙였다.

### 4. Codex 리뷰 — 비해당 (MD 전용)

코드 파일 무변경이라 층 1-4 의 "MD 전용은 Codex 비해당" 그대로다. **'안 돌린 것'이고 '못 돌린 것'이 아니다** — 도구는 이 저장소에서 선다(`node scripts/gov.mjs run-codex-review` · 다리 실측 확인).

### 5. E2E — 비해당 (MD 전용)

변경 파일 5개 전부 `md/` 아래이고 브라우저 동작에 닿지 않는다. **덧붙여 이 앱은 1-5 러너 자체가 없다** — 그 사실을 이번에 `master.md` 에 값으로 세웠다(위 2-(3)).

### 6. 배포 — 비해당 (MD 전용 · 오너 결정 2026-08-19)

**어느 걸음까지 갔나 — 어느 걸음도 안 밟았다.** MD 만 바뀐 회차는 배포 걸음을 안 밟는다는 오늘 결정 그대로다. **미리보기 주소·판번호 없음 — 빈칸이 아니라 비해당이다.**

가지 push 는 했다 — `lane/hizzifix` 신규 가지(`c52f4cc`). 이것은 조각 신호를 위한 저장이고 배포 걸음이 아니다. **곁들여, 이 저장소는 Preview 대상 환경변수가 0이라 그 push 로 Preview 빌드가 서지도 않는다**(2026-08-18 buildfail 실측이 그대로 유효).

- 커밋 해시 `c52f4cc647cadce32ec3ba969e75f9fd7d618eb5`
- todo 갱신 — 함께 커밋(아래 7)
- done 갱신 — **작업 레인이라 안 썼다.** 위 done 줄을 통합 레인이 붙인다.
- 지도 갱신 — **비해당.** 파일 신설·이동·삭제가 없고 기능↔파일 매핑도 안 바뀌었다(`master.md` 안에 절을 세운 것은 그 문서 자신의 내용이다).

### 7. 닫는 todo 항목

**소거한 줄 없음 — 이번 변경은 소거가 아니라 정정이다.** 고친 줄 셋을 원문으로 남긴다.

- `- (거버넌스 잔여) done.md 자기참조 케이스 포맷 규약 — harness.md 3-6 한 줄 추가` → 좌표만 `1-6` 으로.
- `- (거버넌스 잔여) rules-detail.md dangling 참조 정리 — "CLAUDE.md [4-2]·[4-3]·[4-4]·[2]" 거버넌스 재설계 이전부터 부정확. …` → 지목 **10줄 · 파일 다섯**과 무늬·범위로 교체. **옛 등재가 든 `rules-detail.md` 는 이 저장소에 없는 파일이다**(2026-07-31 개명 — `coding-rules-detail.md`).
- `- (거버넌스 잔여) harness.md 3 "현재 spec: playwright-login.spec.js" 문구 실측 반영 — 해당 파일 testDir 밖이라 표준 명령 실행 불가 (2026-04-24 블록 ④ 1-5 관찰)` → **죽은 지목을 걷고 사실만 남겼다.** 층 `harness.md` 에 `playwright-login` **0건**(`grep -c` 실측)이라 그 지목은 가리킬 것이 없다. 그 항목이 말하던 사실은 산다 — 그 파일이 루트에 있고 `testDir: './tests'` 밖이라 어느 줄로도 안 돌아간다. **거버넌스 잔여가 아니라 이 저장소 몫으로 옮겼다.**

**원장 줄 수 — 90 → 90줄**(항목 수 불변 · 정정만). 세 줄이 길어져 바이트는 늘었다.

### 8. 미러 동기화 — 비해당

미러가 이 앱 문서를 안 담는다(층 3-7 표 히찌 칸 "없음 — 미러가 그 앱 문서를 안 담는다"). **작업 레인이라 어차피 안 돌리는 자리이기도 하다**(층 2-3 근거).

### 9. 교훈 반영

**교훈 — 좌표는 모양이 같아도 가리키는 문서가 다를 수 있고, 그것을 가르는 것은 그 문서의 머리다.**

**반영한 자리** — `md/core/master-debt.md` 머리(이 저장소 문서). 그 문서가 "harness.md 를 가리키는 항목 여섯이 있는데 그 문서는 없다"까지는 적고 있었으나 **"층 harness.md 와 다르다"를 안 적어**, 좌표 모양만 본 회차가 그 자리를 층 잔존으로 헛세었다. 그 한 줄과 가르는 값 둘(본문이 "3"을 명령 목록으로 다룬다 · `md/archive/progress-2026-04-O.md:146` 의 별도 증언)을 세웠다.

**층으로 올릴 후보 — 아래 10에 넘긴다.** 이 교훈은 이 저장소에서 났지만 **앱마다 소거된 옛 사본이 있을 수 있는 자리**라 층 몫일 수 있다. 이 레인이 층을 못 만져(다른 레인 점유) 제안만 남긴다.

### 10. 기계 쪽을 늘렸나 — 비해당

스크립트·훅·설정에 검사·무늬·인자·임계를 더한 것이 없다. **방향이 반대인 자리도 없다** — 이번에 고친 것은 앱 문서이고 그것을 읽는 코드가 없다.

**다만 층 표 문안 제안 셋을 여기 남긴다** (층은 `lane/layerfix` 점유라 이 레인이 못 고친다):

1. **`harness.md` 3-7 히찌 `1-6 배포 대조` 칸** — 지금 "없음 → 배포 화면 확인"인데 **판번호 통로가 이 앱에 섰다**(`scripts/stamp-version.js` + `prebuild` + `/version.txt` · 프로덕션에서 `362bfd4741db-1787048347649` 회수). 다만 **자동 판정 러너는 없다** — 손으로 `curl` 을 치고 본문이 판번호인지 사람이 묻는다. 그 갈래를 그대로 적으면 다음 사람이 "없음"으로 읽고 다시 세우지 않는다.
2. **같은 표의 "없음"이 자산인지 통로인지 안 갈린다** — 3-7-(3)이 "없음 ≠ 비해당"은 가르는데 그 한 갈래를 더 가르지 않아, 이 앱 1-5 를 **자산이 없는 것으로** 읽은 회차가 실제로 났다(2026-08-19). 이 앱 쪽은 `master.md` 에 절을 세워 막았고, 층 쪽은 그 표가 답할 자리다.
3. **교훈 하나** — 위 9의 "좌표를 가르는 것은 그 문서의 머리"가 층 `rules.md` R-SURVEY 나 R-MEASURE 옆에 설 부류인지 봐 주십시오. **이 레인은 판단만 내고 자리를 안 정합니다.**

### 11. 우편함 걷기 — 비해당 (작업 레인)

이 조각이 곧 끝났다는 신호라 자기 손으로 걷으면 신호가 사라진다(층 2-3 정지).

---

## 통합 레인이 받을 것 — 미결 셋

**(1) `master-debt.md` 의 harness 부류를 접을지 다시 세울지.** 그 문서가 2026-07-31부터 유보해 둔 자리이고, 이 회차는 **가르는 줄만 세웠지 판정은 안 했다.** 항목 본문 그대로다.

**(2) `CLAUDE.md [N]` 대괄호 dangling 10줄 · 파일 다섯.** 등재 값만 실재에 맞췄고 **푸는 작업은 안 했다**(지시서 범위 밖). 지목 대상이 앱 `CLAUDE.md` 인데 그 문서에 번호 절이 없어, 무엇으로 바꿀지가 그 자체로 결정이다.

**(3) `playwright-login.spec.js` 를 살릴지 옮길지 지울지.** todo 로 옮겨 두었다.

## 이 레인이 잰 것 중 어제 검수와 갈린 값 하나

**어제 (나)에 "잔존 5건"으로 낸 것이 넷이다.** 둘(`master-debt.md:145·153`)은 층이 아니라 **소거된 앱 사본**을 가리켰고, 대신 어제 안 셌던 부류(대괄호 10줄)가 따로 있었다. **어제 그 다섯을 좌표 모양으로만 세고 그 문서 머리를 안 읽은 것이 원인이고, 그것은 (나)가 아니라 (라)에 적혔어야 했다.** 통합 레인도 같은 값을 그대로 오너께 올렸다고 밝혔다 — **양쪽이 같은 자리를 같은 방식으로 틀렸다**(층 `harness.md` 2-6-(5) 가 적는 그 한계다 — 같은 문서를 읽고 같은 추론을 한 자리는 세션이 몇이든 함께 틀린다).
