# previewset — 히찌 (2026-08-18 17:58)

작업지시서 — 전언 · 미리보기 배포 세우기 (앱 셋 · 오너 승인). 이 조각은 히찌 몫이다.

## 1. 탐색·검색

- 저장소 안 — 판번호 무늬(`COMMIT_SHA`·`BUILD_ID`·`version.txt`·`BUILD_VERSION`)가 **0건**. 제 grep 이 낸 0이라 축을 바꿔 다시 쟀다(확장자 지정을 걷고 추적 파일 전수 `git grep -in version`) — 남는 것은 `ci.yml` 의 `node-version`, `*.rules` 의 `rules_version`, `ask-claude.js` 의 `anthropic-version` 뿐이다. `public/` 은 이미지 2개, `package.json` 에 `prebuild` 없음, `vercel.json` **없음**.
- 라우트 핸들러 선례 **0건**(`src/app` 아래 `route.ts` 없음) — 새 패턴을 세우지 않고 정적 파일로 갔다.
- `src/middleware.ts` 는 `hana-vote.vercel.app` 호스트일 때만 재작성하고 그 밖에는 통과라, `/version.txt` 를 막지 않는다(원문 확인).
- 외부 — Vercel 시스템 환경변수 문서 대조: `VERCEL_GIT_COMMIT_SHA` 는 git 커밋이 부른 배포에만 차고 아니면 **빈 문자열**이다. 빌드 명령은 `package.json` 의 `build` 스크립트가 프레임워크 기본(`next build`)보다 앞선다.
- 본 — 르호봇 `scripts/build-sw-config.mjs:138-140`.

## 2. 구현

- `scripts/stamp-version.js` (신설) — `public/version.txt` 에 `{커밋해시 앞 12자}-{빌드 시각}` 을 찍는다. 르호봇은 서비스워커 설정을 겸하는 파일이라 판번호 부분만 가져왔고, 이 저장소는 npm·CommonJS 라 확장자와 문법이 다르다.
- `package.json` — `prebuild` 한 줄.
- `.gitignore` — `/public/version.txt`(빌드 산출물) · `.harness/*.log`(Codex 지적).
- `md/core/master.md` 10장 — 미리보기 주소 받는 줄과 판번호 묻는 줄.

커밋 `e6483de` · 영향 파일 4개.

## 3. 빌드 검증 — PASS

- `npm run build` **exit 0**. `prebuild` 가 실제로 돌아 `public/version.txt` 17바이트가 섰다(`dev-1787042898909`).
- `npm run lint` — 오류 0 · 경고 3건(모두 기존 파일: `ImageViewer.tsx` 의 `<img>`, `RequestDetailPopup.tsx` 의 훅 의존 둘). 이번 변경이 낸 것이 아니다.
- `npm run test:unit` — **48/48 통과** (파일 6개).
- 되살림 **양쪽** — ① `VERCEL_GIT_COMMIT_SHA` 를 실제 해시로 주니 `6d3afbf0c3eb-…` 로 찍혔다. ② Vercel 이 커밋이 아닐 때 주는 **빈 문자열**을 주니 `dev-…` 로 찍혔다(빈 값이 그대로 새어 시각만 남는 갈래를 막는다). ③ 되돌려 확인.
- `git check-ignore -v` 로 `public/version.txt`·`.harness/previewset-hizzi.log` 둘 다 추적 밖인 것을 확인.

## 4. Codex 리뷰 — 통과 (2회차)

1회차 P2 둘, 둘 다 실재라 고쳤다.
- **P2 미리보기 조회가 남의 가지를 문다** — 저장소 전체에서 최신 Preview 를 받으면 병렬 레인에서 남의 배포가 온다. `sha` 필터를 걸도록 고쳤고, **그 필터가 실제로 도는 것을 `gh api` 로 왕복해 확인**했다(Vercel 이 배포 `ref` 에 가지 이름이 아니라 커밋 해시를 넣는다).
- **P2 하네스 기록이 커밋될 자리** — `.harness/*.log` 가 무시 목록에 없어 이 저장소가 문서로 권하는 `git add .` 로 함께 담길 자리였다. 무시 규칙을 더하고 `check-ignore` 로 확인.

2회차 지적 **0건**.

## 5. E2E — 비해당

이 앱의 1-5 는 층 3-7 표에서 **없음 → 오너 육안**이다. 변경 파일 넷 중 화면에 닿는 것이 없다(빌드 스크립트·설정·문서).

## 6. 배포 — 비해당 (작업 레인)

가지 커밋·push 까지다. 머지·배포는 통합 레인 몫이다.

## 7. 닫는 todo 항목 — 해당 없음

지시서 회차라 이 앱 원장에서 닫는 줄이 없다. 작업 레인이라 원장도 안 만졌다.

## 8. 미러 동기화 — 비해당 (작업 레인)

## 9. 교훈 반영

- **지시서가 "이미 돌고 있다"고 적은 것이 절반만 맞았다** — 방아쇠는 서는데 빌드가 전부 실패하고 있었다(아래 11 아래 딸린 실측). 이 자리에서 배운 것은 문서에 넣었다: `master.md` 의 **`state` 를 먼저 본다** 줄이 그것이다. 주소만 읽고 지나가면 실패를 미리보기로 착각한다.
- 규칙 MD 로 올릴 것은 이 조각에 없다 — 층 문서는 이 회차 점유 밖이다.

## 10. 기계 쪽을 늘렸으면 적힌 자리

`node scripts/gov.mjs scan-text "stamp-version|prebuild"` — 58건이 나왔으나 **전부 르호봇 자기 얼개**이고 이 앱의 신설 이름을 가리키는 자리는 0건이다. 적을 자리는 이번에 만든 `master.md` 10장 그 절이다.

**못 따라간 자리 하나** — 층 `harness.md` 3-7 표의 히찌 `1-6 배포 대조` 칸이 아직 **"없음 → 배포 화면 확인"** 이다. 이제 `curl {주소}/version.txt` 가 그 자리를 받는다. **층 문서는 이 회차 점유 밖이라 안 고쳤다** — 통합 레인이 받을 자리다.

## 11. 우편함 걷기 — 비해당 (작업 레인)

---

## 통합 레인에 넘기는 실측 (원장으로 갈 자리)

**히찌 Preview 배포가 오늘 3건 다 실패했다.** `gh api` 로 각 배포의 statuses 를 읽어 확인했다(2026-08-18T02:10:18Z · 02:28:03Z · 03:06:02Z UTC · 셋 다 `state: failure` · 사유는 Vercel 쪽 로그를 봐야 나온다).

- **원인은 못 쟀다.** 이 트리에서 `npm run build` 는 exit 0 이라 코드 쪽 컴파일 실패는 아니다. 남은 갈래는 Vercel 쪽(Preview 환경변수·빌드 설정)인데, 그것을 보려면 `npx vercel inspect --logs` 가 필요하고 그 앞에 프로젝트 잇기가 서야 한다. **이 회차는 `vercel` 쓰기 호출 금지라 안 갔다** — 못 한 것이 아니라 이 수단이 이 회차에서 안 열린 것이다.
- **이 가지도 실패했다 — 넷째다.** push 뒤 40초에 Preview 가 섰고(`5959437255` · 2026-08-18T08:57:53Z) 그것도 `state: failure` 였다. **앞선 셋이 이 회차 변경 이전이라 원인이 이 변경일 수는 없다.** 다만 넷째가 앞선 셋과 같은 원인인지는 로그 없이 못 가른다. GitHub 쪽에는 빌드 로그가 안 실린다 — `log_url` 이 배포 주소 자체이고 사유 문구가 `npx vercel inspect --logs` 를 가리킨다.
- **판번호가 Vercel 에서 실제로 찍히는지도 아직 못 쟀다.** 로컬에서는 `prebuild` 가 돌지만, 그 프로젝트 설정의 빌드 명령이 `next build` 로 덮여 있으면 안 돈다(그 설정은 위와 같은 이유로 못 봤다). **확인법** — 배포가 성공한 뒤 `curl {미리보기주소}/version.txt` 가 200 과 `{해시}-{시각}` 을 내면 선 것이다. 404 면 그 자리를 다시 판다.

**`git add .` 이 이 앱 문서에 배포 절차로 적혀 있다**(`master.md` 10장 CLI 블록). 층의 floor 밴드는 pathspec 커밋을 요구하고 `git add .` 을 금한다(`rules.md` 3장). 이번 회차 점유 밖이라 **안 고쳤고**, 고칠지는 통합·오너 자리다. 위 Codex P2 둘째가 정확히 그 줄이 무는 자리였다.

## done 줄 (통합 레인이 옮길 것)

- [2026-08-18] 히찌 판번호 도장 신설 · 미리보기 주소 받는 줄 (e6483de) — 영향 파일 4개, prebuild 가 public/version.txt 에 커밋해시와 빌드 시각을 찍고 master.md 10장이 그 주소를 받는 줄을 든다.
