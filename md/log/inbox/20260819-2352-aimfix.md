# aimfix — 히찌보드 문서 지목 3건 (개혁 1회차 낙진)

> **오너가 이 셋만 고치라고 하셨다** (2026-08-19). 그 앱은 완전히 갈아엎을 예정이라 **게이트를 넓히는 대상에서는 뺐고**, 이 셋은 그것과 다른 자리다 — **코드가 아니라 층 문서를 가리키는 지목 줄**이고, 갈아엎어도 그 줄은 어차피 다시 써야 한다.
>
> **잰 자리** — 레인 `/Users/chillkim/hizzi-board-wt/aimfix` · 기준 `5b30f22`(기준 가지는 `master` 다).

## 1~2. 탐색·구현

| 자리 | 옛 좌표 | 새 이름 |
|---|---|---|
| `md/core/master.md:115` | `rules.md` 4-2 | `rules.md` 문서 수정 절차 — 도메인 층 |
| `md/core/master.md:389` | `harness.md` 3-7 | `ops-ref.md` 앱별 도구 |
| `md/core/session.md:42` | `rules.md` 4-2 | `rules.md` 문서 수정 절차 — 도메인 층 |

**`master.md:389` 는 한 낱말을 더 고쳤다** — 그 문장이 *"이 앱의 **1-5** 를 없음으로 적는다"* 로 단계를 옛 번호로 불렀다. **새 표에서 그 칸 이름은 `E2E` 다.** 좌표만 고치고 단계 이름을 두면 다음 사람이 없는 번호를 찾는다.

## 3. 빌드 검증

**깨지는 지목 3 → 0**(이 트리에서 낱말로 확인).

**못 쟀다 — 게이트를 이 레인에 못 겨눈다.** 그 검사는 층 아래 앱 폴더를 훑으므로 **머지 전에는 이 워크트리를 원리상 못 본다.** 통합 자리에서 `hizzi-board/` 셋이 0이 되는지 확인해 주어야 한다.

**이 앱의 빌드 검증(`npm run lint`·`build`·`test:unit`)은 안 돌렸다** — 문서 세 줄만 고쳤고 코드에 안 닿는다. **도구 부재가 아니라 공정의 판단이다.**

## 4~6. 리뷰·E2E·배포

**Codex 비해당 — 문서 전용.** **E2E 비해당** — 이 앱은 그 자리가 `없음(통로)` 이기도 하고, 이 변경이 브라우저 동작에 안 닿는다. **배포 비해당.**

## 7. 기록

닫는 todo 해당 없음 · done·미러·우편함 **비해당 — 작업 레인.**

---

## 넘기는 값

**이 앱을 갈아엎는 회차가 알아야 할 것 하나** — `md/core/master.md:389` 는 **이 앱에 E2E 자산이 서 있고 부르는 줄만 없다**는 사실을 담고 있다(설정 한 벌 + spec 32건). 갈아엎을 때 **그 자산을 버릴지 새 화면에 맞춰 살릴지가 결정 자리**다. 이 줄이 그 자리를 여는 신호다.

**안 쟀다** — 이 저장소의 다른 옛 좌표는 안 봤다. 게이트가 무는 셋만 고쳤고, `md/log/done.md` 는 게이트가 기록이라 안 잰다.
